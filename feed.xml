<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://tyy-x.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://tyy-x.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-10T22:52:28+09:00</updated><id>https://tyy-x.github.io/feed.xml</id><title type="html">Bare-Metal Notes</title><subtitle>Personal website of Yuyang Tang — firmware engineer exploring systems, software, and ideas. </subtitle><entry><title type="html">Setting up this site (and the tech that powers it)</title><link href="https://tyy-x.github.io/blog/2026/setup/" rel="alternate" type="text/html" title="Setting up this site (and the tech that powers it)"/><published>2026-05-09T19:28:25+09:00</published><updated>2026-05-09T19:28:25+09:00</updated><id>https://tyy-x.github.io/blog/2026/setup</id><content type="html" xml:base="https://tyy-x.github.io/blog/2026/setup/"><![CDATA[<p>I rebuilt this site from scratch in May 2026. The last time I’d touched it was on a different machine, and al-folio had moved on enough that a clean install felt easier than chasing a rebase. This post is partly the steps I followed and partly notes-to-future-self about the moving parts that make a Jekyll site on GitHub Pages actually work.</p> <h2 id="setup-steps-assuming-a-fresh-start">Setup steps (assuming a fresh start)</h2> <ol> <li> <p><strong>Create the repo from the template.</strong> On GitHub, <em>Use this template</em> on <code class="language-plaintext highlighter-rouge">alshedivat/al-folio</code>, name the new repo <code class="language-plaintext highlighter-rouge">tyy-x.github.io</code>. Personal/user sites have to be named <code class="language-plaintext highlighter-rouge">&lt;username&gt;.github.io</code> — that’s a GitHub Pages requirement, not an al-folio one.</p> </li> <li> <p><strong>Clone it locally.</strong> Standard <code class="language-plaintext highlighter-rouge">git clone</code>.</p> </li> <li><strong>Install Docker.</strong> On Debian 13: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>docker.io docker-compose
<span class="nb">sudo </span>usermod <span class="nt">-aG</span> docker <span class="nv">$USER</span>
<span class="nb">sudo </span>systemctl <span class="nb">enable</span> <span class="nt">--now</span> docker
</code></pre></div> </div> <p>Log out and back in for the group change to take effect. The rationale, command by command:</p> <ul> <li><strong>Docker is a client-server system.</strong> The <code class="language-plaintext highlighter-rouge">docker</code> CLI you type doesn’t do the work itself — it talks to a long-running background process called the <em>Docker daemon</em> (<code class="language-plaintext highlighter-rouge">dockerd</code>). The daemon is what actually pulls images, runs containers, and manages networks. So an “install Docker” really means “install the daemon, install the CLI, and make sure they can talk.” <ul> <li><code class="language-plaintext highlighter-rouge">docker.io</code> package — installs the daemon and the <code class="language-plaintext highlighter-rouge">docker</code> CLI.</li> <li><code class="language-plaintext highlighter-rouge">docker-compose</code> package — installs the Compose v2 binary at <code class="language-plaintext highlighter-rouge">/usr/libexec/docker/cli-plugins/docker-compose</code>, which is what makes the modern <code class="language-plaintext highlighter-rouge">docker compose</code> subcommand work. It also drops a <code class="language-plaintext highlighter-rouge">/usr/bin/docker-compose</code> symlink to the same binary so the older <code class="language-plaintext highlighter-rouge">docker-compose ...</code> (with hyphen) invocation still works. Both paths run the same v2 code; you need this package to get either.</li> </ul> </li> <li><strong><code class="language-plaintext highlighter-rouge">usermod -aG docker $USER</code></strong> — the daemon listens on a Unix socket owned by the <code class="language-plaintext highlighter-rouge">docker</code> group. Adding myself to that group lets me run <code class="language-plaintext highlighter-rouge">docker</code> commands without <code class="language-plaintext highlighter-rouge">sudo</code>. (Trade-off: anyone in <code class="language-plaintext highlighter-rouge">docker</code> group is effectively root-equivalent — they can mount <code class="language-plaintext highlighter-rouge">/</code> into a container and edit anything. Acceptable for my single-user laptop.)</li> <li><strong><code class="language-plaintext highlighter-rouge">systemctl enable --now docker</code></strong> — <code class="language-plaintext highlighter-rouge">enable</code> registers the daemon to auto-start on every boot; <code class="language-plaintext highlighter-rouge">--now</code> also starts it right now. Without <code class="language-plaintext highlighter-rouge">--now</code> I’d have to reboot before Docker worked.</li> </ul> </li> <li><strong>Configure <code class="language-plaintext highlighter-rouge">_config.yml</code>.</strong> Two mandatory fields for a personal site: <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">url</span><span class="pi">:</span> <span class="s">https://tyy-x.github.io</span>
<span class="na">baseurl</span><span class="pi">:</span>    <span class="c1"># MUST stay empty (do not delete this line)</span>
</code></pre></div> </div> <p>Everything else (title, name, profile pic) is taste-driven and can wait.</p> </li> <li><strong>Start the local dev server.</strong> From the repo dir: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose pull <span class="o">&amp;&amp;</span> docker compose up
</code></pre></div> </div> <p>Site appears at <code class="language-plaintext highlighter-rouge">http://localhost:8080</code> with live-reload — edits to most files trigger a rebuild and a browser refresh (<code class="language-plaintext highlighter-rouge">_config.yml</code> is the exception; that one needs a container restart).</p> <p>What each command does:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">docker compose pull</code></strong> reads <code class="language-plaintext highlighter-rouge">docker-compose.yml</code>, finds the <code class="language-plaintext highlighter-rouge">image:</code> reference (<code class="language-plaintext highlighter-rouge">amirpourmand/al-folio:v0.16.3</code>), and downloads it from Docker Hub into the local image cache. For a pinned version tag like this, the image is content-addressed and immutable — so the download only happens once, on first run.</li> <li><strong><code class="language-plaintext highlighter-rouge">docker compose up</code></strong> creates a container from that image, applies the port mappings, volume mounts, and env vars from the compose file, then starts the container in the foreground (add <code class="language-plaintext highlighter-rouge">-d</code> to detach). If the image isn’t already cached, <code class="language-plaintext highlighter-rouge">up</code> will pull it automatically — making <code class="language-plaintext highlighter-rouge">pull</code> technically optional. The pattern in the QUICKSTART (<code class="language-plaintext highlighter-rouge">pull &amp;&amp; up</code>) is just defensive: explicitly fetch any updates first.</li> </ul> <p>Could I do this with plain <code class="language-plaintext highlighter-rouge">docker run</code> instead? Yes — <code class="language-plaintext highlighter-rouge">docker-compose.yml</code> only declares a single service, so there’s nothing compose-specific you couldn’t replicate by hand:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-d</span> <span class="nt">-p</span> 8080:8080 <span class="nt">-p</span> 35729:35729 <span class="se">\</span>
  <span class="nt">-v</span> <span class="s2">"</span><span class="si">$(</span><span class="nb">pwd</span><span class="si">)</span><span class="s2">:/srv/jekyll"</span> <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">JEKYLL_ENV</span><span class="o">=</span>development <span class="se">\</span>
  amirpourmand/al-folio:v0.16.3
</code></pre></div> </div> <p>Functionally equivalent for now. But the YAML file documents the configuration declaratively, version-controlled with the rest of the project, and scales naturally to multi-container setups (e.g., adding a database service later). Compose for anything beyond a one-off <code class="language-plaintext highlighter-rouge">docker run</code>.</p> </li> <li> <p><strong>Push to GitHub.</strong> First push triggers the auto-build pipeline.</p> </li> <li><strong>Point GitHub Pages at the right branch.</strong> Settings → Pages → Source: <em>Deploy from a branch</em> → <code class="language-plaintext highlighter-rouge">gh-pages</code>. Without this step, GitHub will keep trying to build <code class="language-plaintext highlighter-rouge">main</code> itself and fail (more on this below).</li> </ol> <p>After step 7, every <code class="language-plaintext highlighter-rouge">git push</code> to <code class="language-plaintext highlighter-rouge">main</code> rebuilds and redeploys the site automatically.</p> <h2 id="whats-actually-happening-under-the-hood">What’s actually happening under the hood</h2> <p>The setup feels like magic — push, wait three minutes, site is live. But there’s a precise division of labor between Docker, GitHub Actions, the <code class="language-plaintext highlighter-rouge">gh-pages</code> branch, and GitHub Pages. None of it makes sense in isolation. I’ll build up the picture concept by concept.</p> <h3 id="static-sites-and-static-site-generators">Static sites and static site generators</h3> <p>A <strong>static site</strong> is a website made entirely of pre-built files: HTML, CSS, JavaScript, images. The web server’s only job is to send those files to browsers — there’s no database query per request, no per-user computation, no server-side code execution. Every visitor gets the same response (any personalization happens client-side via JavaScript).</p> <p>The contrast is a <strong>dynamic site</strong> like Twitter or Gmail, where the server runs code on every request to assemble the response from databases and user state.</p> <p>Static sites are massively popular for blogs, docs, and portfolios because they’re fast (just file sends), cheap (any static host or CDN), secure (no DB to attack, no server code to exploit), and trivially scalable (a CDN handles 1 visitor or 1M visitors the same way).</p> <p>The catch: writing raw HTML for every blog post is miserable. That’s where <strong>static site generators</strong> (SSGs) come in — tools that turn nice source files (Markdown posts, templates, data files) into a folder of static HTML at <em>build time</em>. You author in Markdown, run the SSG, get a <code class="language-plaintext highlighter-rouge">_site/</code> folder full of plain HTML/CSS/JS ready to serve.</p> <p>The big SSGs: <strong>Jekyll</strong> (Ruby), <strong>Hugo</strong> (Go), <strong>Astro</strong> (JS), <strong>Next.js</strong> (in static-export mode), <strong>Eleventy</strong>, <strong>MkDocs</strong>, etc. al-folio is built on Jekyll.</p> <h3 id="docker-containers-images">Docker, containers, images</h3> <p>Docker packages an application <em>together with everything it needs to run</em> — OS libraries, language runtime, system tools, dependencies — into a self-contained unit called a <strong>container</strong>. The container runs as a normal process on your host, but with its own isolated filesystem, network, and process tree.</p> <p>The vocabulary worth pinning down:</p> <ul> <li>An <strong>image</strong> is a frozen, read-only snapshot of a filesystem with everything pre-installed. Built once from a recipe (a <code class="language-plaintext highlighter-rouge">Dockerfile</code>), shared via a <strong>registry</strong> (Docker Hub being the public default).</li> <li>A <strong>container</strong> is a running instance of an image — like a process, but with its own isolated <code class="language-plaintext highlighter-rouge">/</code>, its own network namespace, its own view of system resources.</li> <li>A <strong>Dockerfile</strong> is the recipe for building an image (<code class="language-plaintext highlighter-rouge">FROM ruby:slim</code>, <code class="language-plaintext highlighter-rouge">RUN apt-get install …</code>, <code class="language-plaintext highlighter-rouge">COPY …</code>).</li> <li><strong><code class="language-plaintext highlighter-rouge">docker-compose.yml</code></strong> is the recipe for <em>running</em> one or more containers with specific configuration (port mappings, volume mounts, env vars).</li> </ul> <p>For my site, this means I never have to install Ruby, Jekyll, ImageMagick, nbconvert, or Bundler on Debian directly. The image <code class="language-plaintext highlighter-rouge">amirpourmand/al-folio:v0.16.3</code> already contains all of them, pre-built by the al-folio maintainers and pushed to Docker Hub. <code class="language-plaintext highlighter-rouge">docker compose up</code> creates a container from that image, mounts my repo into it at <code class="language-plaintext highlighter-rouge">/srv/jekyll</code>, and runs Jekyll inside. I get <code class="language-plaintext highlighter-rouge">localhost:8080</code> working without polluting my host system.</p> <p>The deeper lesson: containers solve the “works on my machine” problem by making “my machine” identical to “their machine” by construction.</p> <h3 id="github-pages">GitHub Pages</h3> <p>GitHub Pages is a static-file web hosting service bundled into GitHub. It serves files from a chosen branch of a repo at <code class="language-plaintext highlighter-rouge">&lt;username&gt;.github.io[/&lt;reponame&gt;]</code> (for a user site). Free for public repos, automatic HTTPS via Let’s Encrypt, custom-domain support, generous bandwidth limits.</p> <p>It also has a <strong>built-in Jekyll capability</strong>: by default, when you point Pages at a branch, it tries to run Jekyll on those files first using its own restricted environment — the <code class="language-plaintext highlighter-rouge">github-pages</code> gem at a fixed version (currently v232), Jekyll 3.10, and a small allowlist of plugins. This is meant as a convenience for people who don’t want to run their own build, but it can’t handle modern Jekyll setups like al-folio (which uses Jekyll 4.x and plugins like <code class="language-plaintext highlighter-rouge">jekyll-toc</code> that aren’t on the allowlist).</p> <p>The relation between Jekyll and GitHub Pages, then: <strong>Jekyll is the SSG; GitHub Pages is the host.</strong> They were designed to work together (GitHub built Pages around Jekyll), but they’re separable. You can use Jekyll without Pages (host the output anywhere) and Pages without Jekyll (serve any static files or use another SSG).</p> <p>al-folio’s strategy is to run its own full Jekyll build, then hand Pages just the finished static output. That’s where CI/CD comes in.</p> <h3 id="cicd--the-idea-behind-the-rest">CI/CD — the idea behind the rest</h3> <p>CI/CD stands for <strong>Continuous Integration / Continuous Deployment</strong>. The core idea: every change you make is automatically built and (if the build passes) released, by a machine that doesn’t share your laptop’s quirks. No “deploy guy”, no manual SSH, no “works on my machine” surprises.</p> <p>For this site, the cycle is:</p> <ul> <li>I push a commit</li> <li>A clean machine on GitHub’s infrastructure clones my repo</li> <li>It builds the site</li> <li>If the build succeeds, the static output is published</li> <li>If it fails, the live site stays at the last good version and I get a red ✗</li> </ul> <p>The platform that runs this for me is <strong>GitHub Actions</strong>.</p> <h3 id="github-actions-and-workflows">GitHub Actions and workflows</h3> <p>GitHub Actions is GitHub’s built-in service for running code on its servers in response to events in a repository (a push, a pull request, a manual click, a scheduled time). It hands you a fresh Ubuntu VM, runs whatever commands you specify, then throws the VM away.</p> <p>To use GitHub Actions, you define <strong>workflows</strong> — automations defined in YAML files under <code class="language-plaintext highlighter-rouge">.github/workflows/</code>. Each file declares:</p> <ul> <li><em>When</em> it runs (<code class="language-plaintext highlighter-rouge">on: push</code>, <code class="language-plaintext highlighter-rouge">on: schedule</code>, <code class="language-plaintext highlighter-rouge">on: workflow_dispatch</code>, etc.)</li> <li><em>What machine</em> it runs on (<code class="language-plaintext highlighter-rouge">runs-on: ubuntu-latest</code>)</li> <li><em>What steps</em> to take, top to bottom</li> </ul> <p>al-folio ships several workflows. The one that matters for deployment is <code class="language-plaintext highlighter-rouge">deploy.yml</code>. Its job, in plain English:</p> <ol> <li>Boot a clean Ubuntu VM</li> <li><code class="language-plaintext highlighter-rouge">git clone</code> my repo into it</li> <li>Install Ruby 3.2.2 and the al-folio gem dependencies</li> <li>Run <code class="language-plaintext highlighter-rouge">bundle exec jekyll build --lsi</code> — produces <code class="language-plaintext highlighter-rouge">_site/</code>, the rendered static HTML/CSS/JS</li> <li>Use the <code class="language-plaintext highlighter-rouge">JamesIves/github-pages-deploy-action</code> to push <code class="language-plaintext highlighter-rouge">_site/</code> as a single commit to a separate branch called <code class="language-plaintext highlighter-rouge">gh-pages</code> (creating the branch if it doesn’t exist; force-pushing to overwrite the previous deploy)</li> </ol> <p>That last step is the bridge to GitHub Pages.</p> <h3 id="gh-pages--the-published-output-branch">gh-pages — the published-output branch</h3> <p><code class="language-plaintext highlighter-rouge">gh-pages</code> is a Git branch like any other, but by convention it holds the <strong>built, ready-to-serve</strong> version of the site, not the source code. Its content is the output of step 4 above: rendered HTML, minified CSS, optimized JS, copied images.</p> <p>The reason for the separation: my <code class="language-plaintext highlighter-rouge">main</code> branch contains Markdown, Liquid templates, SCSS, BibTeX — none of which a browser can render directly. The <code class="language-plaintext highlighter-rouge">gh-pages</code> branch contains the pre-rendered HTML that’s ready to ship.</p> <p>Importantly, <strong>I never edit <code class="language-plaintext highlighter-rouge">gh-pages</code> directly.</strong> The Deploy workflow writes to it; my job is to edit <code class="language-plaintext highlighter-rouge">main</code>. Looking at what the JamesIves action actually does on the runner (visible in the workflow’s Deploy step logs), it:</p> <ul> <li>Checks out an orphan <code class="language-plaintext highlighter-rouge">gh-pages</code> branch in a temporary worktree</li> <li><code class="language-plaintext highlighter-rouge">rsync</code>s <code class="language-plaintext highlighter-rouge">_site/</code> into that worktree (with <code class="language-plaintext highlighter-rouge">--delete</code>, excluding <code class="language-plaintext highlighter-rouge">CNAME</code>, <code class="language-plaintext highlighter-rouge">.nojekyll</code>, <code class="language-plaintext highlighter-rouge">.git</code>, etc.)</li> <li>Commits and force-pushes to <code class="language-plaintext highlighter-rouge">gh-pages</code></li> </ul> <p>Force-push because the previous deploy is irrelevant — the branch isn’t a history of the site, it’s a snapshot of the latest build.</p> <h3 id="the-source-switch-and-the-pages-build-deployment-quirk">The Source switch, and the <code class="language-plaintext highlighter-rouge">pages-build-deployment</code> quirk</h3> <p>Once <code class="language-plaintext highlighter-rouge">gh-pages</code> exists, you tell GitHub Pages where to look: Settings → Pages → Source: <em>Deploy from a branch</em> → <code class="language-plaintext highlighter-rouge">gh-pages</code>. This is the configuration knob that closes the loop.</p> <p>Now here’s the part that confused me. Watching the Actions tab, I saw <strong>two</strong> workflows fire on every push:</p> <ul> <li><strong>Deploy site</strong> (the al-folio one in <code class="language-plaintext highlighter-rouge">.github/workflows/deploy.yml</code>)</li> <li><strong>pages-build-deployment</strong> — which doesn’t appear anywhere in <code class="language-plaintext highlighter-rouge">.github/workflows/</code></li> </ul> <p>That second workflow is <strong>GitHub’s automatic Pages build</strong>, hosted by GitHub, not in my repo. It’s implemented as a Docker-based action (<code class="language-plaintext highlighter-rouge">ghcr.io/actions/jekyll-build-pages:v1.0.13</code>) that GitHub fires whenever the branch configured as Pages source changes. So:</p> <ul> <li>Source set to <code class="language-plaintext highlighter-rouge">main</code> → <code class="language-plaintext highlighter-rouge">pages-build-deployment</code> runs Jekyll on my <strong>source code</strong>. That source code uses al-folio’s plugins (<code class="language-plaintext highlighter-rouge">jekyll-toc</code> etc.) which aren’t on GitHub’s restricted allowlist → <code class="language-plaintext highlighter-rouge">Liquid Exception: Unknown tag 'toc'</code> → <strong>fail</strong>.</li> <li>Source set to <code class="language-plaintext highlighter-rouge">gh-pages</code> → <code class="language-plaintext highlighter-rouge">pages-build-deployment</code> runs Jekyll on my <strong>built output</strong>. That output is plain HTML files with no front-matter, no `` tags, no plugin requirements. Jekyll has nothing to do — it passes the files through to its output and finishes in ~50 seconds → <strong>success</strong>.</li> </ul> <p>It’s the same Jekyll process in both cases. The difference is purely the input: source code that needs plugins to render, vs. pre-rendered HTML that just needs to be copied through.</p> <p>There’s also an explicit way to skip the Jekyll build entirely: a <code class="language-plaintext highlighter-rouge">.nojekyll</code> file at the root of the served branch tells Pages to leave the files alone. al-folio’s deploy doesn’t add one — the no-op behavior happens naturally with pre-built HTML — but it’s the standard signal if I ever want to make the skip explicit.</p> <h2 id="putting-it-together">Putting it together</h2> <p>Here’s the whole pipeline:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>my laptop                          GitHub
─────────                          ──────
docker compose up
  │
  ▼
container w/ Ruby + Jekyll
  │
  ▼
preview at localhost:8080

git push main  ─────────────────▶  Deploy workflow (.github/workflows/deploy.yml)
                                     ┃ runs natively on Ubuntu runner
                                     ┃   (no container)
                                     ┃   bundle exec jekyll build → _site/
                                     ▼
                                   JamesIves action: rsync _site → gh-pages,
                                                     force-push
                                     │
                                     ▼ (branch update triggers…)
                                   pages-build-deployment workflow
                                     ┃ runs in jekyll-build-pages container
                                     ┃   Jekyll over pre-built HTML = no-op
                                     ▼
                                   GitHub Pages serves gh-pages
                                                              ▼
                                                       tyy-x.github.io
</code></pre></div></div> <p>Each box is doing one job. The chain of trust: my local Docker preview matches what CI will produce; the Deploy workflow commits the result to <code class="language-plaintext highlighter-rouge">gh-pages</code>; the gh-pages update triggers GitHub’s auto-build, which essentially no-ops; Pages serves what’s there. Break any link and the site stops updating, but at least it’s clear <em>which</em> link broke.</p> <p>A few clarifications worth highlighting, since they tripped me up:</p> <ul> <li><strong>Docker shows up on my laptop only.</strong> The Deploy workflow runs Jekyll natively on the Ubuntu runner — there’s no <code class="language-plaintext highlighter-rouge">container:</code> field in <code class="language-plaintext highlighter-rouge">deploy.yml</code>. (GitHub’s automatic <code class="language-plaintext highlighter-rouge">pages-build-deployment</code> does use a Docker container, but that’s a separate story.)</li> <li><strong>The Deploy workflow and <code class="language-plaintext highlighter-rouge">pages-build-deployment</code> are independent.</strong> They’re not orchestrated together; the only coupling is that the first writes to <code class="language-plaintext highlighter-rouge">gh-pages</code> and the second is triggered by <code class="language-plaintext highlighter-rouge">gh-pages</code> changing.</li> <li><strong>Pages doesn’t have to use Jekyll at all.</strong> Beyond the <code class="language-plaintext highlighter-rouge">.nojekyll</code> opt-out mentioned above, switching Source to “GitHub Actions” mode bypasses <code class="language-plaintext highlighter-rouge">pages-build-deployment</code> entirely — your workflow takes full responsibility for producing the artifact. al-folio uses neither path; it works out of the box because Jekyll-over-already-built-HTML is harmless.</li> </ul> <hr/> <p>That’s the whole picture as I now understand it.</p>]]></content><author><name></name></author><category term="meta"/><category term="setup"/><category term="jekyll"/><category term="docker"/><category term="ci-cd"/><summary type="html"><![CDATA[A fresh-start install of an al-folio Jekyll site, plus notes on Docker, GitHub Actions, gh-pages, and CI/CD — what each piece does and how they fit together.]]></summary></entry><entry><title type="html">Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra</title><link href="https://tyy-x.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/" rel="alternate" type="text/html" title="Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra"/><published>2024-05-14T09:00:00+09:00</published><updated>2024-05-14T09:00:00+09:00</updated><id>https://tyy-x.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra</id><content type="html" xml:base="https://tyy-x.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/"><![CDATA[<p>Learn more:Learn more:Learn more:Learn more:Learn more:Learn more:May 14, 2024 We’re introducing a series of updates across the Gemini family of models, including the new 1.5 Flash, our lightweight model for speed and efficiency, and Project Astra, our vision for the future of AI assistants. In December, we launched our first natively multimodal model Gemini 1.0 in three sizes: Ultra, Pro and Nano. Just a few months later we released 1.5 Pro, with enhanced performance and a breakthrough long context window of 1 million tokens.Developers and enterprise customers have been putting 1.5 Pro to use in incredible ways and finding its long context window, multimodal reasoning capabilities and impressive overall performance incredibly useful.We know from user feedback that some applications need lower latency and a lower cost to serve. This inspired us to keep innovating, so today, we’re introducing Gemini 1.5 Flash: a model that’s lighter-weight than 1.5 Pro, and designed to be fast and efficient to serve at scale.Both 1.5 Pro and 1.5 Flash are available in public preview with a 1 million token context window in Google AI Studio and Vertex AI. And now, 1.5 Pro is also available with a 2 million token context window via waitlist to developers using the API and to Google Cloud customers.We’re also introducing updates across the Gemini family of models, announcing our next generation of open models, Gemma 2, and sharing progress on the future of AI assistants, with Project Astra.Context lengths of leading foundation models compared with Gemini 1.5’s 2 million token capability1.5 Flash is the newest addition to the Gemini model family and the fastest Gemini model served in the API. It’s optimized for high-volume, high-frequency tasks at scale, is more cost-efficient to serve and features our breakthrough long context window.While it’s a lighter weight model than 1.5 Pro, it’s highly capable of multimodal reasoning across vast amounts of information and delivers impressive quality for its size.The new Gemini 1.5 Flash model is optimized for speed and efficiency, is highly capable of multimodal reasoning and features our breakthrough long context window.1.5 Flash excels at summarization, chat applications, image and video captioning, data extraction from long documents and tables, and more. This is because it’s been trained by 1.5 Pro through a process called “distillation,” where the most essential knowledge and skills from a larger model are transferred to a smaller, more efficient model.Read more about 1.5 Flash in our updated Gemini 1.5 technical report, on the Gemini technology page, and learn about 1.5 Flash’s availability and pricing.Over the last few months, we’ve significantly improved 1.5 Pro, our best model for general performance across a wide range of tasks.Beyond extending its context window to 2 million tokens, we’ve enhanced its code generation, logical reasoning and planning, multi-turn conversation, and audio and image understanding through data and algorithmic advances. We see strong improvements on public and internal benchmarks for each of these tasks.1.5 Pro can now follow increasingly complex and nuanced instructions, including ones that specify product-level behavior involving role, format and style. We’ve improved control over the model’s responses for specific use cases, like crafting the persona and response style of a chat agent or automating workflows through multiple function calls. And we’ve enabled users to steer model behavior by setting system instructions.We added audio understanding in the Gemini API and Google AI Studio, so 1.5 Pro can now reason across image and audio for videos uploaded in Google AI Studio. And we’re now integrating 1.5 Pro into Google products, including Gemini Advanced and in Workspace apps.Read more about 1.5 Pro in our updated Gemini 1.5 technical report and on the Gemini technology page.Gemini Nano is expanding beyond text-only inputs to include images as well. Starting with Pixel, applications using Gemini Nano with Multimodality will be able to understand the world the way people do — not just through text, but also through sight, sound and spoken language.Read more about Gemini 1.0 Nano on Android.Today, we’re also sharing a series of updates to Gemma, our family of open models built from the same research and technology used to create the Gemini models.We’re announcing Gemma 2, our next generation of open models for responsible AI innovation. Gemma 2 has a new architecture designed for breakthrough performance and efficiency, and will be available in new sizes.The Gemma family is also expanding with PaliGemma, our first vision-language model inspired by PaLI-3. And we’ve upgraded our Responsible Generative AI Toolkit with LLM Comparator for evaluating the quality of model responses.Read more on the Developer blog.As part of Google DeepMind’s mission to build AI responsibly to benefit humanity, we’ve always wanted to develop universal AI agents that can be helpful in everyday life. That’s why today, we’re sharing our progress in building the future of AI assistants with Project Astra (advanced seeing and talking responsive agent).To be truly useful, an agent needs to understand and respond to the complex and dynamic world just like people do — and take in and remember what it sees and hears to understand context and take action. It also needs to be proactive, teachable and personal, so users can talk to it naturally and without lag or delay.While we’ve made incredible progress developing AI systems that can understand multimodal information, getting response time down to something conversational is a difficult engineering challenge. Over the past few years, we’ve been working to improve how our models perceive, reason and converse to make the pace and quality of interaction feel more natural.Building on Gemini, we’ve developed prototype agents that can process information faster by continuously encoding video frames, combining the video and speech input into a timeline of events, and caching this information for efficient recall.By leveraging our leading speech models, we also enhanced how they sound, giving the agents a wider range of intonations. These agents can better understand the context they’re being used in, and respond quickly, in conversation.With technology like this, it’s easy to envision a future where people could have an expert AI assistant by their side, through a phone or glasses. And some of these capabilities are coming to Google products, like the Gemini app and web experience, later this year.We’ve made incredible progress so far with our family of Gemini models, and we’re always striving to advance the state-of-the-art even further. By investing in a relentless production line of innovation, we’re able to explore new ideas at the frontier, while also unlocking the possibility of new and exciting Gemini use cases.Learn more about Gemini and its capabilities. Your information will be used in accordance with Google’s privacy policy.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>      Done. Just one step more.
    
      Check your inbox to confirm your subscription.
    You are already subscribed to our newsletter.
    You can also subscribe with a
    different email address
    
    .
    
  Let’s stay in touch. Get the latest news from Google in your inbox.
          Follow Us
</code></pre></div></div>]]></content><author><name></name></author><category term="external-posts"/><category term="google"/><summary type="html"><![CDATA[We’re sharing updates across our Gemini family of models and a glimpse of Project Astra, our vision for the future of AI assistants.]]></summary></entry><entry><title type="html">Displaying External Posts on Your al-folio Blog</title><link href="https://tyy-x.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog/" rel="alternate" type="text/html" title="Displaying External Posts on Your al-folio Blog"/><published>2022-04-24T08:20:09+09:00</published><updated>2022-04-24T08:20:09+09:00</updated><id>https://tyy-x.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog</id><content type="html" xml:base="https://tyy-x.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog/"><![CDATA[<h3>External Posts on Your al-folio Blog</h3> <p>If you prefer publishing blog posts on medium.com or other external sources, starting version v0.5.0, <a href="https://github.com/alshedivat/al-folio">al-folio</a> lets you to display your external posts in the blog feed of your website! 🎉🎉</p> <p>Configuring external sources of super simple. After upgrading to v0.5.0, just add the following section to your _config.yml:</p> <pre>external_sources:<br />  - name: medium.com  # name of the source (arbitrary string)<br />    rss_url: <a href="https://medium.com/@al-folio/feed">https://medium.com/@&lt;your-medium-username&gt;/feed</a></pre> <p>The example above adds your medium.com blog post feed as an external source. But you can add arbitrary RSS feeds as sources.</p> <p>Any questions or suggestions? 👉 Start <a href="https://github.com/alshedivat/al-folio/discussions">a discussion on GitHub</a>!</p> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=b60a1d241a0a" width="1" height="1" alt=""/></p>]]></content><author><name></name></author><category term="external-posts"/><category term="medium"/></entry></feed>